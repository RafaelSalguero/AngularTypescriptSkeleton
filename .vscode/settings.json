// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/.git": true,
        "**/.DS_Store": true,
        "**/*.js": { "when": "$(basename).ts"}
        //"node_modules" : true,
       // "typings" : true
        // ,
        // "app/**/*.js" : true,
        // "app/**/*.js.map" : true
    }
}