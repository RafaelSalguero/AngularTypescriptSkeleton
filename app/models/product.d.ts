declare module models {
    /**Typings for a simulated product web api service */
    interface Product {
        /**Primary key */
        PK: number;
        Description: string;
        UnitPrice: number;
    }
}
