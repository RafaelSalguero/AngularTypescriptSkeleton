declare module models {
    /**Client typings for a simulated client service */
    interface Client {
        /**Primary key */
        PK: number;
        Name: string;
        Age: number;
    }
}
